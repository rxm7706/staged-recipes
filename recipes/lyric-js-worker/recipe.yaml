# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.1.7"

package:
  name: lyric-js-worker
  version: ${{ version }}

source:
  # Vendor-the-blob pattern (itkwasm-downsample-wasi precedent): the PyPI
  # sdist ships a pre-built WASI binary (javascript_worker.wasm) as package
  # data. The WASI engine that executes it is supplied by lyric-py's embedded
  # wasmtime crate at runtime — not a build-time dependency here.
  url: https://pypi.org/packages/source/l/lyric-js-worker/lyric_js_worker-${{ version }}.tar.gz
  sha256: 3c18e565d230fabc1b1d5b08d2a3a9472b40446faa18f43ad03e2f66c5d3022c

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >=${{ python_min }}
    # Sole upstream runtime dep (lockstep release with lyric-js-worker).
    - lyric-task >=0.1.7

tests:
  - python:
      imports:
        - lyric_js_worker
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"
  - script:
      # Confirm the vendored WASI blob shipped in the wheel; do not invoke it
      # (executing the worker would require a full WASI runtime harness).
      - python -c "import os, lyric_js_worker as m; w = os.path.join(os.path.dirname(m.__file__), 'javascript_worker.wasm'); assert os.path.isfile(w), w; print('wasm OK:', os.path.getsize(w), 'bytes')"

about:
  homepage: https://github.com/lyric-project/lyric-runtime
  repository: https://github.com/lyric-project/lyric-runtime
  license: MIT
  # PyPI sdist ships no LICENSE; vendored from the upstream v0.1.7 tag.
  license_file: LICENSE
  summary: Default JavaScript WebAssembly worker for Lyric (pre-built WASI component).

extra:
  recipe-maintainers:
    - rxm7706
